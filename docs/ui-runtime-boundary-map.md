# UI Runtime Boundary Map

Use this map before editing high-traffic shell UI in `scripts/main.gd`. The goal is to keep navigation, home, shop, chat, leaderboard, and profile changes scoped to their owning regions instead of drifting into unrelated helpers.

## Stable Entry Points

| Boundary | Main entry points | Notes |
| --- | --- | --- |
| Main shell/facade contract | `main.gd`: lifecycle callbacks (`_ready`, `_process`, `_input`, `_notification`, `_exit_tree`), `_update_ui` scheduler, runtime/surface accessors, app-wide action lookup facades (`_action_key`, `_action_data`), app theme wrappers, `_clear_page_transient_input_state`; domain owners: `NavigationShell`, `InputRoutingShell`, `SkillDetailSurface`, `SkillSwipeActivitySurface`, `PerformanceRuntime`, `OnboardingRuntime`, and the other surfaces/runtimes named below | This is the intentional remaining `main.gd` shape. Do not extract these shell/facade methods just to reduce line count. Do not move previously extracted ownership back into `main.gd`: frame-refresh predicates belong to `PerformanceRuntime`, detail resume scroll and action-card registry hygiene belong to `SkillDetailSurface`, tutorial flow belongs to `OnboardingRuntime`, and navigation selection belongs to `NavigationShell`. |
| Input routing shell | `InputRoutingShell`: `input`, fishing preflight, primary-button press precheck, top-level/modal/screen/activity routing, fishing method lock routing, action-card press/release routing, fishing detail routing, page-switch global routing, system-back handling, and owned input hit-test helpers; `main.gd`: `_input` wrapper, `_input_routing_shell()` accessor, retained host-owned input helpers, and module action-zone routing wrappers | Owns global input orchestration order, action-card/page-switch/fishing-detail input hit tests, and back-button priority. Domain handlers stay in their current owners: fishing, tutorial, settings, chat/profile, achievements, skill swipe, hub, and button press. Preserve handled-event calls and the order: fishing preflight, top-level controls, modal controls, screen controls, activity surface. |
| Tutorial/onboarding runtime | `OnboardingRuntime`: `_start_tutorial`, `_finish_tutorial`, `_tutorial_check_progress`, swipe/path/accessibility gates, header/stamina sequence orchestration, and tutorial preview/latch helpers; `TutorialOverlaySurface`: overlay build/input/target lookup plus onboarding fight header/stamina/stat presentation, auto-run note placement/cleanup, and header/stat fades; `main.gd`: `_resolve_detail_lazy_stack()` and non-onboarding visual tip helpers | Owns first-run tutorial/onboarding scalar state, progression gates, swipe access policy, and graduation. Overlay presentation stays in `TutorialOverlaySurface`; deferred onboarding sequence callers should target `OnboardingRuntime`, not main wrappers. Preserve save keys and first-run swipe/stamina/lock/passive/silver tip behavior. |
| Home and navigation shell | `main.gd`: `_build_home_page`, `_show_skills`, `_show_shop`, and page-switch compatibility wrappers; `NavigationShell`: `_build_nav_bar`, `_ensure_nav_bar_icons`, `_nav_button`, `_nav_style`, `_apply_nav_style`, `_route_bottom_nav_button_global_input`, `_activate_bottom_nav_target`, `_sync_nav_new_symbol_dot`, `_queue_page_switch_transition`, `_process_page_switch_pending_transition`, and pinned/queue page entry/return | Owns the first screen, bottom navigation, and top-level screen switching. `NavigationShell` owns bottom-nav chrome/factory/icon hydration/open-icon swapping, build/routing/visibility, input hit tests, lock checks/messages, red-X return routing, new-symbol dots, seen-id save bridge, unlock fade state, page-switch render-cover queue/release state, and pinned/queue page entry/return ownership; `main.gd` keeps gameplay unlock predicates, page functions, page state, page-switch button construction/press animation, physical cover drawing, and retained scene-call compatibility wrappers. Disabled page stash/cache restore paths were deleted in Step 52; transitions clear live content directly. Preserve `current_screen` values, bottom-safe-area behavior, nav seen-id save key, locked text, and no-preview-flash page-switch cover behavior. |
| Module utility shell | `NavigationShell`: `_build_module_utility_row`, `_route_module_utility_button_global_input`, `_toggle_module_sort_menu`, `_module_utility_return_screen_for_current`, `_module_utility_return_detail_scroll_for_current`, `_show_pinned_activities`, `_return_from_pinned_activities`, `_show_activity_queue`, `_return_from_activity_queue`, `_return_from_skills_utility`, `_sync_module_utility_row_visibility`, skill-swipe utility-row fade/drag alpha helpers, module utility/sort refs, collapse/motion state, and utility nav press-state helpers; `SkillDetailSurface`: card-local module pin/collapse zones, badges, and hit helpers; `main.gd` keeps compatibility properties | Owns the module utility row, pinned/queue page entry/return, return routing helpers, sort menu presentation/input/animation, and action-card-local module chrome. Sort preference/save state, module pin/collapse card state, and global input ordering stay host-owned. Preserve bottom UI hit ordering with chat strip and bottom nav. |
| Recovery action-card chrome | `SkillDetailSurface` and `SkillSwipeActivitySurface`: recovery depth/background/progress-rail shaping; `RecoveryModules`: recovery contract/target/application/result copy; `main.gd`: compatibility facades only | Recovery gameplay policy belongs to `RecoveryModules`; recovery visuals stay in the existing action-card UI surfaces. Preserve `recovery.stamina`/`recovery.target` data shape, Wrap Hands `+3`, wide-U rail/card bottom, and no extra `recovery_label`. |
| Buildable action-card chrome | `SkillDetailSurface`: buildable overlay/title CTA calls; `BuildableModuleOverlay`: overlay widget factory; `BuildableModules`: key/buildable/built checks, save-shape normalization, build cost/label/affordability/transaction policy; `SaveRuntime`: `"built_modules"` payload/restore calls | Buildable gameplay policy, transaction, keying, and built-state normalization belong to `BuildableModules`; buildable overlay presentation stays in `SkillDetailSurface`/`BuildableModuleOverlay`. Preserve build costs, material spend, built-state save shape, Building XP reward, result text, and render refresh. |
| Skill swipe preview surface | `SkillSwipeActivitySurface`: `_build_skill_swipe_preview_page`, `_update_skill_swipe_preview_states`, `_queue_skill_swipe_preview_prewarm`, preview/prewarm/cache helpers; callers go through `_skill_swipe_activity_surface()` | Owns preview page/card construction, preview state, parked pages, prewarm, placeholder, light-preview, and real-card-cache helpers. Gesture tracking, physical cover handoff/fade drawing, detail-finalize, and core detail-lazy runtime stay in `main.gd`; page-switch render-cover queue/release state lives in `NavigationShell`. |
| Hub surface | `HubSurface`: `_hub_mission_badge`, `_sync_hub_mission_badge`, `_play_hub_mission_completion_icon_ceremony`, action-card badge help/popover helpers; callers use owner-local calls or `host._hub_surface()` | Owns hub mission action-card badge presentation and completion icon-pop ceremony. Mission data/progress rules stay in `HubRuntime`/host state; real action cards keep lazy `mission_badge_parent` metadata and allocate badges only when targeted by a mission. |
| Achievement overlay surface | `AchievementOverlaySurface`: `_update_achievements_ui`, `_queue_home_achievement_refresh`, `_achievement_medal_slot_strip`, `_route_achievement_medal_press`, `_route_achievement_medal_popover_dismiss`, `_hide_achievement_medal_popovers`, modal tab/rebuild/layout helpers, global-buff rows, achievement log cards, and achievement art; `AchievementState`: live milestone/medal/reward state helpers; `main.gd` keeps overlay open/close compatibility and routes update/input calls | Overlay surface owns home best-activity refresh, per-skill medal strip construction/refresh, medal hit testing, popover parent/layout/runtime, achievement modal rebuilding, global-buff tab rows, log cards, and achievement art. Achievement state helpers belong to `AchievementState` and read host state directly. Preserve modal input ordering and home popover parenting to visible `home_page`. |
| Reward feedback surface | `RewardFeedbackSurface`: `_set_result`, `_play_action_feedback`, `_play_action_mastery_feedback`, `_float_reward`, `_start_reward_float_tween`, `_play_activity_crit_feedback`, visible-card lookup helpers; `main.gd` keeps public/direct-call reward facades, while activity-crit tween callbacks are owner-local | Owns generic result text, action success/failure feedback, crit overlays/text bursts, XP/mastery/level-up floats, tired/stamina/warning floats, and reward-float cleanup. Domain-specific fish and thieving reward floats stay in their existing owners. |
| Material collection surface | `MaterialCollectionSurface`: `_build_mat_collection_row`, `_mat_collection_module`, `_sync_mat_collection_card`, `toggle_berry_mode`, `set_berry_mode_enabled`, `toggle_berry_prep_for_action`, `sync_berry_mode_overlay`, `sync_berry_prep_badges`; `main.gd` keeps only the surface factory | Owns material collection module presentation, material reward flyer/pulse feedback, Berry Mode UI state, prep button callback target, badge sync, and full-screen berry border overlay. Material wallet, Berry Prep save shape, matching, consume, and result text stay in `MaterialRuntime`. Preserve `berry_prep_buttons`, `berry_prep_hint_label_id`, and `berry_prep` save key. |
| Generic button press runtime | `ButtonPressState`: press metadata helpers, generic depress/release tweening, depressed-button tracking, default button SFX debounce/gating, pointer-left release cleanup; `main.gd` keeps compatibility bridges | Owns normal `BaseButton` press animation/audio behavior. Activity/action-card shell depth press state stays separate in `SkillSwipeActivitySurface` and host `depressed_activity_shell_buttons`; bottom-nav transition hold remains metadata-driven through the generic runtime. |
| App performance runtime | `PerformanceRuntime`: `_configure_performance_mode`, `_process_battery_governor`, `_record_battery_governor_activity`, `_performance_overlay_toggle_text`, `_set_performance_overlay_enabled`, `_consume_ui_static_refresh`, `_skill_detail_needs_high_frequency_ui_update`, `_skill_detail_has_fishing_camera_returning`; `main.gd`: scheduler caller and `ui_static_refresh_elapsed` compatibility property | Owns frame caps, mobile battery governor state, adaptive VSync setup, debug-only performance overlay boot, static-refresh throttling, high-frequency detail refresh predicates, and `PerfMonitor` loading. Keep `main.gd` as the scheduler; do not reintroduce these predicates there. |
| App boot warmup runtime | `BootWarmupRuntime`: `_build_boot_warmup_overlay`, `_show_boot_warmup_overlay`, `_hide_boot_warmup_overlay`, `_dismiss_boot_splash_for_play`, `_boot_texture_warmup_background`; `main.gd` keeps compatibility wrappers/properties | Owns startup splash presentation, progress text/bar updates, minimum visible timing, guarded hide tween, and background texture warmup frame budget. Leave boot texture path ownership and detail-card texture prewarm in `main.gd` until their domain owners are split deliberately. |
| God Mode/test-state setup | `TestStateRuntime`: `_god_mode_*_state`, `_apply_art_review_test_unlock_all_state`, `_clear_*_test_state`; `main.gd` keeps wrappers, `_god_mode_available`, status copy, and controls | Owns dev/test save-state mutations only. Keep release gating, button/status UI, and direct scene-call compatibility in `main.gd`; domain runtimes keep fishing, hub, thieving, temporary-event, achievement, and save ownership. |
| Shop and ads | `_show_shop`, `_shop_unlocked`, rewarded-ad callbacks | Keep ad behavior and release/test ad IDs out of readability-only refactors. Device validation is needed for real ad changes. |
| Chat transport | `_chat_stream_connect`, `_chat_send`, `_chat_apply_stream_payload`, `_chat_sort_and_trim_rows` | Owns Firebase chat I/O, rate limits, and row normalization. Preserve cost-safety and auth guard behavior. |
| Chat presentation | `ProfileChatOverlaySurface`: `_build_chat_strip`, `_build_chat_overlay`, `_chat_strip_visible_on_current_screen`, `_chat_expanded_row`, `_chat_expanded_composer`, `_process_chat_keyboard_lift`, `_update_chat_keyboard_preview`, `_sync_chat_unread_dot`; `main.gd` keeps `_close_chat_overlay` and the surface factory | Owns the collapsed strip, expanded overlay, unread display, composer, and mobile keyboard lift/preview behavior. Chat send/transport/draft state stays host-owned. |
| Leaderboard networking and data | `OnlineRuntime`: Firebase config/auth, cloud-save fetch/upload/status, `_leaderboard_fetch_category`, `_leaderboard_submit_scores`; `LeaderboardState`: `categories`, `score_for_category`, submit readiness/status, save/fetch metadata normalization; `main.gd` keeps `_online_runtime()`, `_refresh_leaderboard_if_visible()`, and profile/style compatibility points | Protected by leaderboard cost-safety and Firebase config/runtime guard tests. Do not bypass cache, submit cooldowns, profile-claim requirements, or saved category-key compatibility. |
| Leaderboard page | `LeaderboardPresentation`: `_render_leaderboard_page` and internal layout helpers; `main.gd` keeps `_leaderboard_category_selected` and shared style/theme helpers | Owns visible leaderboard layout. Keep category IDs, Firebase keys, and profile fields compatible. |
| Profile/avatar UI | `ProfileChatOverlaySurface`: `_build_profile_overlay`, `_rebuild_profile_overlay_if_visible`, `_profile_avatar_picker_button`, `_profile_avatar_texture`, `open_profile_overlay`, `profile_avatar_frame`; leaderboard presentation calls the surface through `host._profile_chat_overlay_surface()` | Owns display-name entry, avatar choice, avatar render/frame styling, and profile status copy. Preserve saved avatar/profile keys. |

## Working Rules

- Refactor one boundary at a time and keep behavior/visual changes out of naming or movement commits.
- If a function crosses boundaries, leave a note in this map before moving it so the next agent understands the coupling.
- Run `.\scripts\check-ui-boundary-contracts.ps1` after edits in these regions, then run the narrower Firebase, chat, or visual checks that match the boundary.
- Avoid committing pre-existing feature work from the dirty tree as a boundary refactor. Stage only the files and hunks owned by the current package.
