# Activity UI Boundary Map

Use this map before editing activity detail pages, action cards, mastery medals, unlocks, offline summary UI in `scripts/ui/achievement_overlay_surface.gd`, stat bonus panel UI in `scripts/ui/skill_detail_surface.gd`, and offline reward simulation in `scripts/gameplay/action_runtime.gd`. These regions are tightly coupled to save data, lazy rendering, mobile gestures, and visual performance, so behavior-neutral readability work should name the boundary before moving code.

## Stable Entry Points

| Boundary | Main entry points | Notes |
| --- | --- | --- |
| Activity data catalog | `ActivityDataCatalog`: `load_action_data`, `load_activity_database`, `rebuild_action_lookup`, `sort_activity_actions_for_page`; `main.gd`: public aliases | Catalog owns the live skill/action collections and sort/lookup behavior. `main.gd` aliases stay as the public identity for existing UI/save/gameplay callers. |
| Skill detail shell | `_render_skill_detail`, `_detail_stack_entry`, `_build_detail_lazy_plan`, `_build_detail_jump_arrows`, `_add_activity_back_arrow` | Owns the scroll stack, lazy card plan, jump arrows, and back navigation. Protected by skill-detail geometry/performance checks. |
| Action cards | `SkillDetailSurface`: `_build_detail_interactive_action_card`, `_detail_action_card_shell`, `_detail_action_card_body`, `_detail_action_stat_widgets`, `_detail_action_mastery_widgets`, `_detail_action_progress_widgets`, `_detail_action_convergence_overlay`, `_detail_action_buildable_overlay`, module pin/collapse action-zone chrome and hit helpers; `ActivityCardStyles`: `action_card_background_edge_underlay`, `activity_card_depth_layer`, `prism_connector_overlay`, `activity_card_shade_layer`, `ensure_activity_card_shade`; `main.gd`: `_activity_card_root_height`, `_activity_card_preview_root_height` | `SkillDetailSurface` owns repeated action-card assembly and card-local pin/collapse chrome/hit detection. Action-card chrome factories live in `ActivityCardStyles`. Height helpers, gameplay math, runtime state, and save decisions stay host-owned. Avoid visual tweaks during naming-only changes. |
| Detail lazy mount/unmount | `_sync_detail_lazy_visible_cards`, `_detail_lazy_unmount_item`, `_detail_lazy_mount_initial_window_sync` | Lazy card mount/unmount mechanics live in `SkillDetailSurface`; scroll-window scans and plan mutation/rebind orchestration stay in `main.gd`. |
| Fishing detail scroll perf/render culling | `FishingUiSurface`: `_process_fishing_scroll_perf_probe`, `_process_fishing_scroll_mode`, `_sync_fishing_detail_render_culling`; `main.gd`: state | Scroll-mode mouse-pick suspension, perf probe accounting, and render-cull visibility/cache behavior live in `FishingUiSurface`; lazy mount planning/window orchestration and state vars stay host-owned. |
| Stat bonus panel | `_sync_activity_stat_popup`, `_ensure_activity_stat_bonus_panel`, `_activity_stat_bonus_details` | Panel rendering/details live in `SkillDetailSurface`; gameplay math remains host-called from existing systems. |
| Activity lock overlay | `SkillDetailSurface`: `_activity_lock_overlay`, `_on_activity_lock_clicked`, `_play_activity_requirement_lock_dismissal`, `_sync_activity_lock_overlay`; `main.gd`: scene-call wrappers | Generic lock overlay/click/dismissal presentation lives in `SkillDetailSurface`; readiness, save/runtime unlock state, fishing method ceremonies, and cropped padlock assets stay host-owned. |
| Pinned/queue active shelf | `NavigationShell`: `_pinned_activities_active_shelf`, `_activity_queue_active_shelf`, `_pinned_activities_empty_state`, `_sync_pinned_active_shelf`, `_finish_pinned_active_shelf_fade_out`; `main.gd`: scene-call wrappers/state | Shelf presentation, empty-state decor, sizing, theme, fade/height tween, and stamina gauge chrome live in `NavigationShell`; queue runtime, pinned data, scroll input, and card builders stay host-owned. |
| Passive and special modules | `_build_passive_module_card`, `_build_thieving_heist_card`; `FishingUiSurface`: `_build_fishing_area_module`, `_fishing_area_module_shell`, `_fishing_area_runtime_layers`, `_mount_fishing_area_method_cards`, `_build_fishing_offer_module`, `_route_fishing_offer_button_global_input`, `_fishing_offer_button_hit`, `_play_fishing_offer_collected_transition`; `main.gd`: fishing offer economy/runtime state | Fishing area assembly/presentation and fishing offer module construction/input live in `FishingUiSurface`; selected location defaults, purchase/save handlers, fish currency, collected flags, result text, wallet/equipped state, and broader fishing economy/runtime state stay host-owned. |
| Mastery medals | `_mastery_level`, `_mastery_progress_pct`, `_mastery_medal_texture`, `_mastery_medal_visual_texture`, `_mastery_for_save` | Mastery is both UI and save data. Preserve canonical action keys and fallback textures. |
| Unlocks and lockpads | `_unlock_padlock_tint_mask_texture`, `_action_has_pending_unlock_readiness`, `_apply_pending_activity_unlock_readiness` | Unlock UI touches manual unlock state, pending ceremony state, and auto-unlock behavior. |
| Offline summary | `AchievementOverlaySurface`: `_maybe_show_offline_summary`, `_offline_summary_activity_card`, `_offline_summary_stat_card`, `_offline_summary_mastery_row`, `_offline_summary_unlock_card`; `main.gd`: `_close_offline_summary_overlay` scene-call wrapper | Owns the away-progress overlay presentation while `main.gd` keeps save/load state, reward math, and direct-call close compatibility. |
| Offline rewards | `ActionRuntime`: `_apply_offline_active_action`, `_offline_active_cycle_seconds`, `_offline_xp_reward`, `_offline_mastery_reward`, `_offline_unlocked_actions` | Gameplay math lives in `ActionRuntime`; save/offline callers route through `host._action_runtime()`. Do not mix math changes into visual refactors. |

## Working Rules

- Keep activity-card construction, lazy render plans, and save payload changes in separate commits unless one change cannot work without the other.
- When renaming locals, prefer domain names such as `lazy_entry`, `render_record`, `activity_def`, `action_id`, `module_key`, and `unlock_requirement`.
- Run `.\scripts\check-activity-ui-boundary-contracts.ps1`, `.\scripts\test-activity-card-geometry.ps1`, `.\scripts\test-save-normalization.ps1`, and the relevant skill-detail checks after edits in these areas.
- Do not rename save keys, action IDs, skill IDs, module UI keys, or generated activity data fields without compatibility handling.
