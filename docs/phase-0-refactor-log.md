# Phase 0 Refactor Log

Chronological notes for the v0.5.0 Phase 0 cleanup pass. Source plan: `docs/plan-v0.5.0.html`.

## 2026-06-28

- Started Phase 0 by reading the v0.5.0 plan and confirming the cleanup scope: audit `scripts/main.gd`, prove dead code with `rg`, collapse repeated module/action checks when justified, improve confusing names, and preserve behavior.
- Recorded initial worktree state before edits: `AGENTS.md` is modified and `docs/plan-v0.5.0.html` is untracked; both appear to be pre-existing user work and will be left intact.
- Measured `scripts/main.gd` at 72,643 lines, confirming this needs a thorough multi-pass walkthrough rather than a single small cleanup.
- Simplified the leaderboard fetch completion path in `scripts/main.gd`: `_on_leaderboard_fetch_completed()` now delegates final storage to `_leaderboard_finalize_fetch_rows()`, removing duplicate inline row-storage logic and restoring the existing total-level compatibility fetch branch.
- Added small detail-lazy module kind helpers in `scripts/main.gd` and replaced repeated string-list checks across cached mount, live mount, unmount, and repair paths. This keeps the rendering branches intact while giving action-backed, fishing-module, and module-kind checks one shared vocabulary.
- Removed exact-reference dead helpers from `scripts/main.gd`: obsolete boot texture prewarm/dismiss wrappers, unused fishing debug/input predicates, unused shutdown free helpers, the old synchronous `_build_ui()` path, and unused cloud/chat helpers. Follow-up `rg` found no active references; only a negative regression assertion still names the old boot-dismiss helper.
- Shortened three internal sentence-like locals in `scripts/main.gd` without changing any serialized or public names: `prev_thieving_restore`, `animate_squeeze_root`, and `waiting_for_nav_target` now replace longer one-block variable names.
- Ran a second symbol-frequency pass after removals and deleted the newly confirmed dead `_ensure_settings_overlay()` wrapper. The active settings path uses the rendered settings screen, while profile/achievements/offline overlays still keep their lazy overlay builders.
- Updated `scripts/test-performance-regressions.ps1` so its thieving scroll preservation assertion follows the renamed local while still checking the same save/restore-around-render behavior.
- Updated the lazy-unmount cached-root regression assertion to require `_detail_lazy_kind_is_fishing_module(kind)`, preserving the same non-fishing cache reuse guard after the module-kind helper extraction.
- Updated lazy unmount regression assertions to check `_detail_lazy_kind_is_module(kind)` plus the helper body, so heist and fishing-area unload coverage remains explicit without duplicating the kind list in the unmount function.
- Validation: `.\scripts\check-leaderboard-cost-safety.ps1` passed, `git diff --check -- scripts\main.gd docs\phase-0-refactor-log.md` passed with only the repository's CRLF warning, and `.\scripts\test-performance-regressions.ps1` passed.
- Validation: `.\scripts\check-project.ps1` passed. The skills-page performance gate failed all three non-strict attempts and continued with `skills-page-performance-release-warning`, matching the existing non-strict behavior; the run also printed known save-regression/shutdown leak warnings. Post-validation process check found no remaining `Godot.exe` processes.
- Screenshot note: no new screenshot was captured because this Phase 0 pass did not intentionally change player-visible layout, text, module shapes, animation, or art. Existing headless visual/smoke checks in `check-project.ps1` passed; any future visual/UI change should still capture a real game screenshot through the safe wrapper.
- Phase 0 closeout: `scripts/main.gd` now measures 72,477 lines, down from the initial 72,643. Phase 0 tasks are covered: `main.gd` was audited in multiple passes, dead helpers were removed only after exact-reference searches, repeated lazy module/action kind checks were collapsed into domain helpers, long internal locals were shortened, and behavior-changing work was limited to reconnecting the existing leaderboard finalization path.
- Continued the dead-code sweep after user review. Removed 21 zero-reference helpers from `scripts/main.gd`, including unused home summary builders, stale hub helpers, unused leaderboard/chat page cards, old lazy-detail title/cache helpers, and no-op navigation completion wrappers. Follow-up `rg` found no remaining references to the removed names. `scripts/main.gd` measured 72,110 lines after this batch.
- Ran multiple cascade passes over private helpers in `scripts/main.gd` and removed another 159 declaration-only helpers. Biggest retired clusters: old test/god-mode panel controls, pinned-activities shelf transition scaffolding, collapsed module shell fragments, unused thieving/heist ornament helpers, old skill-swipe/onboarding completion wrappers, fishing wallet/location/card remnants, dead stylebox factories, and obsolete audio/bootstrap wrappers.
- Extended the sweep beyond `main.gd` and removed zero-reference helpers from `scripts/activity_lock_cluster.gd`, `scripts/ui/action_art_animation_rect.gd`, `scripts/ui/activity_card_depth.gd`, `scripts/ui/blue_guy_chicken_brawl_stage.gd`, `scripts/ui/convergence_multi_progress_bar.gd`, `scripts/ui/firepit_warmth_overlay.gd`, `scripts/ui/fishing_tool_wallet_overlay.gd`, and `scripts/ui/hub_path_dots.gd`.
- Cleaned stale current-doc references so architecture docs no longer point at removed entry points for audio bootstrap, fishing tool menu rendering, chat/settings wrappers, unlock refresh, or pin transition blockers. Historical notes in this refactor log still intentionally record removed names.
- Removed the last declaration-only scratch helpers from `.codex-tmp` probe scripts after the broader code/test/config scan exposed them. These were unused debug paths, not active validation entry points.
- Validation scan: token-counted all non-doc project files (`.gd`, scenes/resources/config, PowerShell/Python tests, and JSON) and found `TOTAL 0` private GDScript functions that appear only at their declaration, after excluding Godot/plugin virtual callbacks. `scripts/main.gd` now measures 69,476 lines, down 3,167 lines from the initial 72,643.
- Validation: `git diff --check` passed for touched code/docs with only normal CRLF warnings, `.\scripts\test-performance-regressions.ps1` passed, and `.\scripts\check-project.ps1` passed. The full project check repeated the existing non-strict skills-page performance warning plus known save-block/shutdown leak warnings, and the required post-Godot process check found no remaining `Godot.exe` processes.
- Broadened the dead-reference search beyond private helpers. Removed declaration-only public/state/global leftovers: `set_fast_flat_render_enabled()` from `RoundedTextureRect`, `dark_mode_overlay`, `fishing_scroll_fast_art_active`, the unused chicken-brawl hero HP color constant, and the unused `FightingModuleStage` global `class_name` registration while preserving the live path-based inheritance.
- Cleaned current text docs that still named removed or completed rename targets: fishing tool rack wording, naming-audit pending-readiness/lazy-entry targets, and one historical stabilization note that preserved an exact removed helper name.
- Validation scan: non-doc code/config/test files now report `TOTAL 0` for declaration-only signals, functions, constants/vars, and class names. The script-path scan found no orphan `scripts/*.gd` files, and the current text-doc scan found no stale backtick/code-block private symbol references outside archives/art-source and this chronological log. `scripts/main.gd` now measures 69,474 lines.
- Validation: `git diff --check` passed for the second-pass edits with only normal CRLF warnings, `.\scripts\test-performance-regressions.ps1` passed, and `.\scripts\check-project.ps1` passed. The full project check again printed the existing non-strict skills-page performance warning and save/shutdown/resource cleanup warnings; the required post-Godot process check found no remaining `Godot.exe` processes.
- Checked for dead references that are not simple declarations: runtime-only versus test/doc-only symbols, custom signal declarations with no emits, orphan script paths, unused global `class_name` registrations, and quoted `res://` paths. Removed unused global class registrations from `scripts/fishing_attempt_bar.gd`, `scripts/fishing_fluid_strip.gd`, `scripts/ui/boot_flex_loading_animation.gd`, and `scripts/ui/blue_guy_chicken_brawl_stage.gd`; at that time all four scripts remained live through existing preload/path references. A later pass deleted `scripts/fishing_attempt_bar.gd` after its last preload was removed.
- Validation scan: declaration-only signals/functions/constants/vars/class names still report `TOTAL 0`, no orphan `scripts/*.gd` paths were found, and refined quoted resource-path scanning found no missing required runtime paths. The only real-looking missing resource is the guarded AdMob development override config path, which is intentionally optional behind `FileAccess.file_exists()`.
- Validation: `git diff --check` passed for the third-pass edits with only normal CRLF warnings, `.\scripts\test-performance-regressions.ps1` passed, and `.\scripts\check-project.ps1` passed. The full project check again printed the existing non-strict skills-page performance warning plus save/shutdown cleanup warnings; the required post-Godot process check found no remaining `Godot.exe` processes.
