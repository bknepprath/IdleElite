Image-generated continuous-body v1. This is the latest targeted edit pass from true image generation, packaged with shared scale and fixed exported baseline. Candidate strengths: four unique frames, exercise-sheet push-up orientation, feet-left/head-right, body/leg seam greatly reduced versus prior prompt-only strips. Review concern: low frames still have a dark lower outer contour that could read as an underside divider; accept only if viewed as outer silhouette, not body-to-leg seam.
