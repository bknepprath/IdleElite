Temporary event module runtime art lives here.

Use `actions/` for event button/action art and `backgrounds/` for event background plates. Only approved, sliced PNGs should be wired into `docs/activity-database.json`.

Keep prompts, contact sheets, and source composites under `docs/art-source/asset-sources/content/events/`.
